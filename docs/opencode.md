# Using with OpenCode

OpenCode supports custom providers. Point it at this proxy to get both Claude and Gemini models in one interactive picker.

```bash
mkdir -p ~/.config/opencode
# See full config example in original docs - baseURL: http://localhost:8080/v1
```
