# Advanced Configuration

See config.example.json and environment variables: PORT, API_KEY, WEBUI_PASSWORD, DEBUG, DEV_MODE, FALLBACK, CLAUDE_CONFIG_PATH, OAUTH_CALLBACK_PORT, HTTP_PROXY.

Full details in original upstream docs.
